// Frontend stub for SMTP registration email. Replace with real API integration.
export async function sendRegistrationEmail({ email, company, phone }) {
  console.log('sendRegistrationEmail stub', { email, company, phone })
  return { ok: true, message: 'Welcome email queued (stub via SMTP)' }
}
